{
    "name": "bowling-layout-translator",
    "version": "2.0.0",
    "private": true,
    "description": "Bowling layout and PAP translation with a dimensioned drilling chart and 3D preview",
    "scripts": {
        "test": "node --test tests/*.test.js",
        "check": "node --check app.js && node --check i18n.js && node --check layout-math.js && node --check drilling-math.js && node --check drilling-chart.js && node --check visualizer.js && npm test"
    }
}
